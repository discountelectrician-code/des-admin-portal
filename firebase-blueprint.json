{
  "entities": {
    "user": {
      "title": "User Profile",
      "description": "Represents a user in the Discount Electrical Service portal with their custom claims permissions.",
      "type": "object",
      "properties": {
        "uid": { "type": "string", "description": "The unique Firebase Auth UID of the user." },
        "email": { "type": "string", "description": "The email address of the user." },
        "displayName": { "type": "string", "description": "The display name of the user." },
        "createdAt": { "type": "string", "format": "date-time", "description": "When the profile was created." },
        "updatedAt": { "type": "string", "format": "date-time", "description": "When the profile was last updated." },
        "claims": {
          "type": "object",
          "description": "The Custom Claims assigned to the user for modular access.",
          "properties": {
            "admin": { "type": "boolean", "description": "Has access to Admin Portal." },
            "pay": { "type": "boolean", "description": "Has access to Payments module." },
            "timecard": { "type": "boolean", "description": "Has access to Timecard module." }
          },
          "required": ["admin", "pay", "timecard"]
        },
        "employeeProfile": {
          "type": "object",
          "description": "Optional extended HR onboarding profile data fields.",
          "properties": {
            "hireDate": { "type": "string" },
            "payRate": { "type": "number" },
            "techLevel": { "type": "string", "enum": ["Apprentice", "Journeyman", "LLE", "Master"] },
            "homeAddress": { "type": "string" },
            "cellPhone": { "type": "string" },
            "driversLicense": { "type": "string" },
            "photoUrl": { "type": "string" }
          }
        }
      },
      "required": ["uid", "email", "claims"]
    },
    "tracking_event": {
      "title": "Tracking Event",
      "description": "Represents a telemetry data log from any of the subdomains.",
      "type": "object",
      "properties": {
        "id": { "type": "string", "description": "Unique event document ID." },
        "timestamp": { "type": "string", "format": "date-time", "description": "The server timestamp when the event occurred." },
        "eventType": { "type": "string", "enum": ["auth", "payment", "timecard", "system", "error"], "description": "The type of telemetry event." },
        "subdomain": { "type": "string", "enum": ["admin", "pay", "timecard"], "description": "The subdomain application releasing this event." },
        "userId": { "type": "string", "description": "UID of the user associated with this event." },
        "userEmail": { "type": "string", "description": "Email of the user associated with this event." },
        "message": { "type": "string", "description": "A descriptive message detailing the telemetry event." },
        "status": { "type": "string", "enum": ["info", "success", "warning", "error"], "description": "Log severity/status." },
        "details": { "type": "string", "description": "Additional JSON metadata or details as string." }
      },
      "required": ["id", "timestamp", "eventType", "subdomain", "userId", "userEmail", "message", "status"]
    },
    "payment_config": {
      "title": "Payment Config",
      "description": "System config representing the selected payment provider.",
      "type": "object",
      "properties": {
        "provider": { "type": "string", "enum": ["stripe", "square"], "description": "The chosen active payment gateway." },
        "updatedAt": { "type": "string", "format": "date-time", "description": "When the payment gateway select state was saved." },
        "updatedBy": { "type": "string", "description": "User email or UID who toggled the provider." },
        "stripeSecretKey": { "type": "string", "description": "Encrypted or masked API private secret key for Stripe." },
        "squareAppId": { "type": "string", "description": "API application identifier for Square." },
        "squareAccessToken": { "type": "string", "description": "API private access token for Square." }
      },
      "required": ["provider"]
    },
    "invite": {
      "title": "Onboarding Invitation",
      "description": "Represents an active, secure onboarding ticket designed to pre-seed technician claim records.",
      "type": "object",
      "properties": {
        "email": { "type": "string" },
        "name": { "type": "string" },
        "role": { "type": "string" },
        "payRate": { "type": "number" },
        "status": { "type": "string", "enum": ["pending", "claimed"] },
        "createdAt": { "type": "string", "format": "date-time" },
        "claims": { "type": "object" },
        "cellPhone": { "type": "string" },
        "driversLicense": { "type": "string" },
        "homeAddress": { "type": "string" },
        "hireDate": { "type": "string" },
        "photoUrl": { "type": "string" },
        "claimedAt": { "type": "string", "format": "date-time" },
        "claimedByUid": { "type": "string" }
      },
      "required": ["name", "cellPhone", "status", "createdAt"]
    },
    "service_request": {
      "title": "Service Request Lead",
      "description": "Represents public electrical service request lead and booking drafts.",
      "type": "object",
      "properties": {
        "id": { "type": "string" },
        "firstName": { "type": "string" },
        "lastName": { "type": "string" },
        "phone": { "type": "string" },
        "email": { "type": "string" },
        "status": { "type": "string", "enum": ["draft", "abandoned", "confirmed"] },
        "createdAt": { "type": "string", "format": "date-time" },
        "serviceDetails": { "type": "string" },
        "techAssignee": { "type": "string" }
      },
      "required": ["firstName", "phone", "email", "status"]
    },
    "admin_settings": {
      "title": "Admin Settings",
      "description": "System settings containing API keys for Google Maps and DataForSEO.",
      "type": "object",
      "properties": {
        "gmaps_api_key": { "type": "string", "description": "The API key for Google Maps." },
        "dataforseo_auth_key": { "type": "string", "description": "The Base64 authentication key for DataForSEO." },
        "updatedAt": { "type": "string", "format": "date-time", "description": "When the settings were last updated." },
        "updatedBy": { "type": "string", "description": "User email or UID who updated the settings." }
      },
      "required": []
    },
    "customer": {
      "title": "Customer",
      "description": "Represents a customer in the CRM.",
      "type": "object",
      "properties": {
        "firstName": { "type": "string" },
        "lastName": { "type": "string" },
        "email": { "type": "string" },
        "phone": { "type": "string" },
        "address": { "type": "string" },
        "status": { "type": "string" },
        "notes": { "type": "string" },
        "company": { "type": "string" },
        "source": { "type": "string" },
        "createdAt": { "type": "string", "format": "date-time" },
        "updatedAt": { "type": "string", "format": "date-time" }
      },
      "required": ["firstName"]
    },
    "seo_scan": {
      "title": "SEO Heatmap Scan",
      "description": "Represents persistent historical GMB grid ranking search results.",
      "type": "object",
      "properties": {
        "id": { "type": "string", "description": "The unique document ID representing the scan." },
        "serviceArea": { "type": "string", "description": "The city or service area of the scan." },
        "keyword": { "type": "string", "description": "The target search keyword analyzed." },
        "targetPlaceId": { "type": "string", "description": "The Google Maps place identifier of the business." },
        "timestamp": { "type": "string", "format": "date-time", "description": "When the scan was initiated." },
        "gridNodes": {
          "type": "array",
          "description": "A collection of individual geographic grid nodes comprising ranks.",
          "items": { "type": "object" }
        }
      },
      "required": ["serviceArea", "keyword", "gridNodes", "timestamp"]
    }
  },
  "firestore": {
    "users/{userId}": {
      "schema": "user",
      "description": "Stores user profiles and their assigned claims/modules permissions."
    },
    "tracking_events/{eventId}": {
      "schema": "tracking_event",
      "description": "Secure collection storing telemetry tracking events from all subdomains in the Discount Electrical ecosystem."
    },
    "settings/payment_config": {
      "schema": "payment_config",
      "description": "Stores the central payment gateway toggle setting."
    },
    "settings/admin_settings": {
      "schema": "admin_settings",
      "description": "Stores the central API credentials and keys for Google Maps and DataForSEO."
    },
    "invites/{inviteId}": {
      "schema": "invite",
      "description": "Stores secure pending and claimed technician onboarding invitation profiles."
    },
    "service_requests/{requestId}": {
      "schema": "service_request",
      "description": "Stores service booking requests showing complete or draft/abandoned status leads."
    },
    "customers/{customerId}": {
      "schema": "customer",
      "description": "Stores CRM customers."
    },
    "seo_scans/{scanId}": {
      "schema": "seo_scan",
      "description": "Stores GMB grid metric scans for geographical search optimization."
    }
  }
}
